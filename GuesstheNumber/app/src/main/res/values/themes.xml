<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.GuessTheNumber" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/navyblue</item>
        <item name="colorAccent">@color/darkblue</item>
        <item name="android:textColor">@color/white</item>

        <!-- Secondary brand color -->
        <item name="android:colorSecondary" tools:targetApi="n_mr1">@color/mustard</item>
        <item name="android:textColorSecondary">@color/black</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/colorPrimary</item>

        <!-- Customize your theme here -->
    </style>

</resources>
